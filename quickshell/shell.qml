// Quickshell entry point — placeholder scaffold.
// Quickshell ships zero default UI; this file is intentionally near-empty.
// Build the bar, launcher, notification daemon, and wallpaper here later.
// Docs: https://quickshell.outfoxxed.me/

import Quickshell

ShellRoot {
    // TODO: bar, launcher (Super+D via `qs ipc call launcher toggle`),
    // notification daemon (replaces mako), wallpaper (replaces swaybg).
}
